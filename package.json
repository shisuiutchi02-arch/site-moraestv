{
  "name": "moraestv-site",
  "private": true,
  "description": "Landing + dashboard: JSON local, Netlify Blobs, ou Postgres (Supabase) com login no dashboard.",
  "scripts": {
    "start": "node server.js",
    "netlify:dev": "npx --yes netlify-cli dev",
    "migrate:supabase": "node scripts/migrate-json-to-supabase.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.5",
    "@supabase/supabase-js": "^2.49.1",
    "pg": "^8.13.1"
  }
}
